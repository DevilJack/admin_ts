export interface IConfig {
    port: string;
}

export interface IUser {
    email: string;
    name: string;
    password: string;
    avatarUrl: string;
    gender: string;
    ageRange: string;
    role: string;
}